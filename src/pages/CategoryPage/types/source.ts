export type SourceSite = 'vinted' | 'vestiaire_collective' | 'other';

export interface SourceStyle {
  bg: string;
  hoverBg: string;
  text: string;
  hoverText: string;
  border: string;
  hoverBorder: string;
}